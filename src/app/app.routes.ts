import { Routes } from '@angular/router';
import PokemonListComponent from './components/pokemons-list/pokemons-list.component';

export const routes: Routes = [
    {
        path : "",
        component : PokemonListComponent
    }
];
